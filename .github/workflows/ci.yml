name: CI

on:
  pull_request:
    branches:
    - master
  push:
    branches:
    - master

jobs:
  build:
    runs-on: gradle@4.10-jdk8
    
    steps:
    - uses: actions/checkout@master

    - name: Run tests
      run: gradle test